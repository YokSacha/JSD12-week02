MongoDB data is collected in post-it style and categorized in a folder.
Each post-it/document has specific ID to identify.
Due to the collected system, the change and update process is much easier and more flexible.
the command using in MongoDB is similar to JS. 

find() > show all
findOne() > show one
countDocuments() > show number
limit() > show the request amount
$eq > equal
$gt > greater than
$gte > greater and equal than
$lt > less than
$lte > less and equal than
$in > including
$match > WHERE in SQL ?????
$group > GROUP BY in SQL ???
